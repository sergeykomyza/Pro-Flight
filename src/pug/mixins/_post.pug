mixin post(img, date, title, text)
	article.post 
		figure.post__img
			img(src="img/" + img, alt= title)
			.post__date #{date}
		.post__info 
			h6.post__title #{title}
			p.post__text #{text}
			a.post__button(href="article.html") читать подробнее